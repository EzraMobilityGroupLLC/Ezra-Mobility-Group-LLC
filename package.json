{
  "name": "ezramobilitygroup",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler pages deploy dist --project-name ezramobilitygroup"
  },
  "dependencies": {
    "astro": "^5.2.0",
    "resend": "^6.16.0"
  },
  "devDependencies": {
    "@astrojs/sitemap": "^3.2.0"
  }
}
